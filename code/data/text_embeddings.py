"""
Text-based task feature embeddings — frozen lookup only.

Each dataset loader defines its own LABEL_NAMES mapping (label → text).
This module is kept as a minimal placeholder; all label/text definitions
live in the individual loader files (e.g. loaders/eegmat.py) for
extensibility.
"""
